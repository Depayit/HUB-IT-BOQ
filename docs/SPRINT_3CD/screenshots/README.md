# Sprint 3C/3D Screenshots

Run after `DATABASE_URL` is set:

```bash
npm run db:seed
node scripts/seed-sprint-3cd-demo.mjs
npm run dev
node scripts/capture-sprint-3cd-screenshots.mjs
```

Set `BASE_URL` or `DISCIPLINE_PAGE_URL` if the dev server is not on port 3000.
